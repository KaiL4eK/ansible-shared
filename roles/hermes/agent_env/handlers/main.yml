---
- name: Restart Hermes Agent gateway
  become: true
  become_user: "{{ hermes_agent_env_user }}"
  ansible.builtin.command:
    argv:
      - "{{ hermes_agent_env_binary_path }}"
      - gateway
      - restart
  environment:
    HOME: "{{ hermes_agent_env_user_home }}"
    HERMES_HOME: "{{ hermes_agent_env_home }}"
  register: hermes_agent_env_gateway_restart_result
  changed_when: hermes_agent_env_gateway_restart_result.rc == 0
  failed_when: hermes_agent_env_gateway_restart_result.rc != 0
